PORT=4000
NODE_ENV=development
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/pet_access"
JWT_SECRET="change-me-in-production-use-32-chars-min"
FRONTEND_ORIGIN="http://localhost:5500"

# Railway (set in dashboard, not locally):
# NODE_ENV=production
# DATABASE_URL=<from Postgres plugin>
# JWT_SECRET=<long random secret>
# FRONTEND_ORIGIN=https://your-app.up.railway.app
